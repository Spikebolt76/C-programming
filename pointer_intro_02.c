#include <stdio.h>

//print string without space, with the help of using pointers and 2 strings
void main()
{
    char str1[100], str2[100];

    printf("\nEnter a string: ");
    gets (str1);
    char *ptr, *ptr1;

    //printf("%d ", &str1);
    ptr = str1;

    for (; *ptr = '\0';)
    {
        
    }

}