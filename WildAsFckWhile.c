#include <stdio.h>

int main()
{
    int num, digit, rem, sum;

    printf("\nEnter a number of five digits: ");
    scanf("%d", &num);

    while(num != 0)
    {
        rem = n % 10;
        
    }
    printf("%d", sum);
}