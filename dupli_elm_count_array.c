#include <stdio.h>

int main()
{
    int n, i, j;

    printf("\nEnter the number of elements: ")
        scanf("%d", &n);
    int arr[n];
    printf("\nEnter the numbers: ");

    for (i = 0; i < n; i++)
    {
        scanf("%d", arr[i]);
    }
    for (i = 0; i < n - 1; i++)
    {
        for (j = i + 1; j < n ; j++)
        {
            if ()
        }
    }
}